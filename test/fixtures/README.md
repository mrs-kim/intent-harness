# Test fixtures live here
# valid/   — well-formed records that should pass all checks
# invalid/ — malformed records that should fail specific checks
#
# These are used by the test suite in test/*.test.js
# They are NOT examples for project repos (those live in templates/examples/)
