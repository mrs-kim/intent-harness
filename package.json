{
  "name": "intent-harness",
  "version": "0.4.0",
  "description": "A requirement harness for AI-assisted product development. Schema-typed records, agent-assisted authoring, human-approved truth.",
  "main": "src/intent.js",
  "bin": {
    "intent": "./src/intent.js"
  },
  "scripts": {
    "test": "node --test test/ids.test.js && node --test test/graph.test.js && node --test test/validate.test.js && node --test test/flip-legitimacy.test.js && node --test test/fetcher.test.js",
    "test:watch": "node --test --watch test/",
    "validate-self": "INTENT_ROOT=. node src/validate.js",
    "lint": "node -e \"require('./src/intent.js')\" -- validate"
  },
  "files": [
    "src/",
    "templates/",
    "README.md"
  ],
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {},
  "keywords": [
    "requirements",
    "product",
    "intent",
    "workflow",
    "ai-assisted",
    "specification",
    "agents"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_ORG/intent-harness"
  }
}